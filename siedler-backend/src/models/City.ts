// models/City.ts

export interface City {
    id: string;
    playerId: string;
    position: number; // Position auf dem Spielfeld (z. B. Kachelnummer)
    // Weitere stadtbezogene Eigenschaften können hier hinzugefügt werden
  }
  
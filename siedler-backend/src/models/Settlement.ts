// models/Settlement.ts

export interface Settlement {
    id: string;
    playerId: string;
    position: number; // Position auf dem Spielfeld (z. B. Kachelnummer)
    // Weitere siedlungsbezogene Eigenschaften können hier hinzugefügt werden
  }
  